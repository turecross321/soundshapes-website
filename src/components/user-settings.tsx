import { FC } from "react";

interface UserSettingsProps {}

const UserSettings: FC<UserSettingsProps> = ({}) => {
  return (
    <div className="relative content content-padding flex justify-center">
      YO MAN
    </div>
  );
};

export default UserSettings;
